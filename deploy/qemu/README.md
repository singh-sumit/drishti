QEMU integration scaffolding for Drishti v0.1.

Place architecture-specific kernel and initramfs images in this directory:
- vmlinuz-x86_64 / rootfs-x86_64.img
- vmlinuz-aarch64 / rootfs-aarch64.img

Then run:
  ./run.sh x86_64
