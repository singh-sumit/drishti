{
  "id": null,
  "uid": "drishti-overview",
  "title": "Drishti Overview",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "tags": ["drishti", "overview"],
  "time": {"from": "now-15m", "to": "now"},
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "CPU Runtime by Process",
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "sum by (comm) (rate(drishti_cpu_run_time_ns_total[1m]))", "refId": "A"}
      ],
      "gridPos": {"x": 0, "y": 0, "w": 12, "h": 8}
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "RSS by Process",
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "topk(10, drishti_mem_rss_bytes)", "refId": "A"}
      ],
      "gridPos": {"x": 12, "y": 0, "w": 12, "h": 8}
    },
    {
      "id": 3,
      "type": "timeseries",
      "title": "Process Lifecycle Events",
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "sum by (event) (rate(drishti_proc_lifecycle_total[1m]))", "refId": "A"}
      ],
      "gridPos": {"x": 0, "y": 8, "w": 12, "h": 8}
    },
    {
      "id": 4,
      "type": "timeseries",
      "title": "System Memory",
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "drishti_mem_available_bytes", "legendFormat": "available", "refId": "A"},
        {"expr": "drishti_mem_cache_bytes", "legendFormat": "cache", "refId": "B"},
        {"expr": "drishti_mem_total_bytes", "legendFormat": "total", "refId": "C"}
      ],
      "gridPos": {"x": 12, "y": 8, "w": 12, "h": 8}
    },
    {
      "id": 5,
      "type": "timeseries",
      "title": "Network Throughput by Interface",
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "sum by (iface) (rate(drishti_net_tx_bytes_total[1m]))", "legendFormat": "tx {{iface}}", "refId": "A"},
        {"expr": "sum by (iface) (rate(drishti_net_rx_bytes_total[1m]))", "legendFormat": "rx {{iface}}", "refId": "B"}
      ],
      "gridPos": {"x": 0, "y": 16, "w": 12, "h": 8}
    },
    {
      "id": 6,
      "type": "timeseries",
      "title": "Disk IOPS by Device",
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "sum by (device) (rate(drishti_disk_iops_total[1m]))", "refId": "A"}
      ],
      "gridPos": {"x": 12, "y": 16, "w": 12, "h": 8}
    }
  ]
}
